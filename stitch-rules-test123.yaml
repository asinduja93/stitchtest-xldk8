kind: Rules
metadata:
  namespace: k8s-freemarker
spec:
  - name: "k8s.AddFreemarkerLabels-02"
    condition:
      deployedType: k8s.Resources
    processor:
      - type: freemarker
        description: "Adding first label to kubernetes spec-02-dryrun"
        phase: POST_FLIGHT
        merge:
          type: overlay
        parameters:
          template: |
            { "metadata" : {
               "labels": {
                 "labelToAdd": "initialValue-dryrun-02"
               }
              }
            }
